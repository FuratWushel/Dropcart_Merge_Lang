{% extends "layouts/layout.html.twig" %}

{% block content %}

    <h1>{{ page_title }}</h1>
    <p class="lead">
        {{ lang.page_home.lead_text|raw }}
    </p>
    <div class="container">
        <div class="col-md-6">
            <p>
                {{ lang.page_home.left_block }}
            </p>
        </div>
        <div class="col-md-6">
            <p>
                {{ lang.page_home.right_block }}
            </p>
        </div>
    </div>

{% endblock %}